# Unblock Agent (human-ops gate for money path)

Spawned when **money_loop** is ranked #1 but product Phase 0.1 cannot advance
because operator credentials/egress are missing.

| Role | Owns | Does not own |
|------|------|--------------|
| **Unblock** | Living checklist, env probe results (honest), plane docs | Spending real funds without human; inventing settle |

## Checklist (update each tick)

| Item | Status | Evidence |
|------|--------|----------|
| Funded Base Sepolia wallet | unknown | |
| Test USDC | unknown | |
| Facilitator reachable | unknown | probe optional |
| `VERITAS_RPC_URL` set | unknown | env |
| Public TLS host | optional | |
| PyPI trusted publisher | optional | |

When all **required** rows are ready, confer Overseer: singular NEXT = Phase 0.1.

```
PROPERTY: unblock agent surfaces human gates without fake green
EVIDENCE LEVEL: L1 (checklist) / L0 (readiness)
NOT PROVEN: settle success
```
