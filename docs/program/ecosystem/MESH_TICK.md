# Ecosystem mesh tick (coordinate via Overseer)

Run as support burst (not product claim):

1. Overseer skims cosystem/BUS.md + CONFERRAL (already in 8m tick).
2. Parallel or serial track ticks per CONTINUOUS.md ecosystem section.
3. Plane bootstrap once: register agents + mint VAAT stipends + issue visas.

`ash
python -m veritas.plane_bootstrap
`
