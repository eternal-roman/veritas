# Ladder transcript — the five reported blockers, climbed (2026-08-09)

Per [`MIND.md`](../../MIND.md) §3: "blocked" is a claim requiring a dated
failing probe. A watcher reported five blockers; each was probed, and none
survived as reported. Evidence inline; nothing below restates a fact it can
point to.

## 1. "Product NEXT — Overseer correctly HOLD, no singular unblocked bet named"

**Diagnosis:** the HOLD rested on blocker 2 (env unset = money path blocked),
which was a hypothesis, not a probe result.
**Cleared:** with the money-path gates probed live (below), the recommended
singular bet is named in `STATE.md` NEXT and `../OVERSEER_CONFERRAL.md`:
**Phase 0.1-R, routine money loop** — scripted settle→reconcile cycles from
any egress host, keeping G9 reconciliation routine instead of historic.
Overseer still owns the claim; what changed is that a bet is *nameable* with
evidence.

## 2. "This host — VERITAS_RPC_URL / facilitator unset, can't re-dogfood here"

**Diagnosis:** unset ≠ unreachable. Both counterparties are public endpoints.
**Built (rung 4):** pinned public defaults now ship in code —
`chain_reconcile.DEFAULT_PUBLIC_RPC_URLS` (testnets only; env always wins;
mainnet always requires the explicit variable so a real-money tx can never be
classified against the wrong chain), consumed by `veritas-ops
reconcile-chain` (per-row `rpc_source` stamped) and by
`python -m veritas.unblock_probe`, which now probes the defaults instead of
reporting "no" on unset.
**Found on the way:** the probe itself carried the field-note-1 defect — no
User-Agent, so Cloudflare 403'd both defaults live. Fixed
(`veritas-unblock-probe/<version>`), witness-tested.
**Proof (this host, env fully unset):** RPC probe `eth_chainId=0x14a34`,
facilitator `/supported` HTTP 200 — `CHECKLIST.md` beside this file, sources
labelled. Any host with egress gets the same result by running the probe;
a host whose probe *fails* has a real, dated block to report.

## 3. "Scale of money — mainnet 0, unsolicited buyers 0, G9 still open as ops gap"

**Probed and narrowed:** repeatability is no longer open — settlement **n=2**
(run 2: tx `0x9ec7edd5bd0a14496f72dd24b78ec65778b7afca600aa06025971ca46a5d352c`,
Base Sepolia, buyer `0xF355…8599` → pay-to, settled after an honest `refused`
verdict with custody root delivered — billable per the outcome taxonomy).
Both settlements chain-confirmed via the **default** RPC path with the env
unset (`../../fable/settlement/chain_reconcile_default_run1.json`, `…run2.json`).
G9 moves from "ran once" toward routine: `veritas-ops reconcile-chain` now
works from a bare environment. **Still true and not claimed otherwise:**
mainnet 0 (human residue: pay-to decision + explicit RPC), unsolicited
buyers 0 (Stage-1 public existence is the path there).

## 4. "Stage-1 public existence — PyPI / TLS / mainnet pay-to = human ops"

**Probed:** the human residue is minutes, and the 90% is already prepared:
`.github/workflows/release.yml` exists — tag-triggered, hash-pinned tooling,
SBOM, Trusted Publishing, deliberately inert until the PyPI side is
configured. Package name `veritas-research` verified **free** on PyPI
(2026-08-09, `GET /pypi/veritas-research/json` → 404).
**Human minutes list (standing):** ① create the `veritas-research` project +
trusted-publisher entry on PyPI, then push tag `v<version>`; ② public TLS
host decision; ③ mainnet pay-to + explicit mainnet RPC decision. Everything
else on this path is agent work and done.

## 5. "Agent loop waste — many ticks true noop_idle or restock thrash until #116"

**Diagnosis:** two different things. `noop_idle` under free+HOLD is the law
working (`MIND.md` — a truthful noop is better than a hygiene PR). The
thrash class (#113 restating stale facts) is what `MIND.md` §5
(pointers, not restatements) exists to kill; #116 corrected it.

```
PROPERTY: each reported blocker either cleared with dated live evidence or narrowed to its true human residue with the 90% prepared
EVIDENCE LEVEL: L1 (probes, settlement JSON, chain reconcile output, PyPI 404 — all dated 2026-08-09)
NOT PROVEN: mainnet settlement; unsolicited buyers; that future probes keep passing (re-probe, never trust this file's date)
```
