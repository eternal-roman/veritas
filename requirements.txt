pydantic>=2.0
fastapi>=0.110
uvicorn>=0.27

# Zero-key web search. Optional: without it the retriever reports
# `dependency_missing` and falls back to the DuckDuckGo Instant Answer API
# rather than silently returning no results.
ddgs>=6.0
